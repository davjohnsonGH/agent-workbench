# Copy to .env and adjust as needed.

# Matches infrastructure/docker/docker-compose.yml
DATABASE_URL=postgres://workbench:workbench@localhost:5432/workbench

# Anthropic credentials for real agent runs (tests use a fake provider).
ANTHROPIC_API_KEY=
# Optional: override the default model (claude-opus-5).
# AGENT_MODEL=
